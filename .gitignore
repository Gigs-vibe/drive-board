# PyInstaller
build/
dist/
*.spec

# Готовый установщик: раздаётся через GitHub Releases, а не через репозиторий.
# Раньше он коммитился каждый релиз — 10 копий по 23 МБ раздули историю до 228 МБ.
installer/
get-pip.py

# Python
__pycache__/
*.pyc
*.pyo

# Local data (user files, not for repo)
drive-board.json
drive-notified.json

# IDE / internal
.claude/
pc-next-session-prompt.md

# WebView2 local storage (created at runtime)
*.json
!requirements.txt
preview_icon.png
preview_fg.png
